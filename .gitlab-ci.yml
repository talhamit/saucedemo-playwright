image: mcr.microsoft.com/playwright:v1.42.1-jammy

stages:
  - test

playwright_tests:
  stage: test
  script:
    - npm install
    - npx playwright install --with-deps
    - npx playwright test
  artifacts:
    when: always
    paths:
      - playwright-report/
